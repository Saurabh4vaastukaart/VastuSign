# Used only when building the mobile client with --dart-define.
API_BASE_URL=https://api.your-domain.example

# Backend runtime values.
AUTH_SECRET=replace-with-at-least-32-random-characters
DATABASE_PATH=./backend/data/vastusign.sqlite
PORT=8787
